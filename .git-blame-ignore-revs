# Commits that changed formatting only, with no change in behavior. git blame skips these
# so a line's blame points at whoever last changed its meaning, not at the reformat.
#
# GitHub honors this file automatically. Locally it needs one command per clone:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# Only add a commit here if it is verifiably behavior-preserving.

# Format the tracked scripts with ruff format
6046db7c92cbd7113e5e3c801abe8a5da533bffa
